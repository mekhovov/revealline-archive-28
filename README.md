# RevealLine archive28: v0.61.21 plus proposed v0.61.22

Retain the exact original v0.61.21 and v0.61.22 releases. The existing v621 cohort and metadata remain byte-identical. New v622 is published release 391751801, source `17e0a455f76c3f4a72f2e596397a0e73463335a5`, tree `e8a544161dc23e67995f21d6bc20d268e4174120`, annotated tag `43885ec8f1a5521547a81c8d2bbc925099561b30`. No game code, artwork, source qualification or original ZIP is rebuilt or changed.

The append expects **1,400 files / 626,845,129 bytes**, leaving **173,154,871 bytes** under the unchanged 800,000,000-byte cap. Of 701 prior rows, **698 release rows plus two support rows remain unchanged**: 700 preserved rows. Only root `index.html` changes to add v622; `.nojekyll` and `releases/index.html` remain exact. **699 new v622 rows / 313,448,826 bytes** are added. Counts come from complete original metadata inventories; no historical payload download is required locally.

The v621 archive was accepted at main `888cd5c5699af7399f8716183482be90adec87c9` (tree `9a43107bf83f77985e77cdd83be73dc4b67520cf`): 701/701 public bodies passed with no retries/failures. Its source checkout `f313814c535bdc622758a90fa521d1f6b38188bd` has the same accepted tree. Native scope was Standard keyboard Team setup/real flight/cuts/Pause/Resume, Solo and release-explorer navigation, with no archive win/picture-dialog, saved-attempt replacement, physical/offline/audio/BFCache or isolated-storage claim. Original main621 earned-picture acceptance remains separate.

At this preparation checkpoint, v622 immutable release and exact-source qualification/native observation exist; its public-main acceptance is pending. **This append is only a local candidate: no new archive commit, hosted run, deployment, public byte or native acceptance is claimed.** No main selector/allocation/admission changes occur here. Current limitations include narrow Large HUD/readability cases and the broader P03/P05/P07/P08/P18 gates.

The workflow fetches both locked tags explicitly: `refs/tags/v0.61.21:refs/tags/v0.61.21` and `refs/tags/v0.61.22:refs/tags/v0.61.22`. The accepted extractor at `a13ab970222498d7c5fa7f62f9fc04fe436979d5` and prepare/verify helpers stay unchanged, with main-only gating, non-cancelling concurrency, permissions and 3 GiB hosted capacity. Keep the 800 MB/20,000-row limits and original tag/source/ZIP CRC/hash checks plus independent full artifact reread.

Root must review the 11 exact candidate paths, commit/merge once, observe the single automatic push workflow and inspect its bounded original receipt. Before admission, audit every expected public body with unchanged before/after authorities, reconcile all 700 unchanged rows and the single index exception, and record scoped real retention of both editions with saved-data ownership respected. Main public acceptance and archive acceptance are separate. Neither workflow success nor this local fixture pass admits v622.
